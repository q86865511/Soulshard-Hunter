# R29 美術跟進輪 · 驗收報告

> 依據：外部複審 `docs/reviews/ART_REAUDIT_2026-08-30.md`（R28 後，66→79/100、無 P0/P1、剩 4 項 P2）
> 分支：`claude/r29-art-followup`（自合併後的 `main` 0d164cd 開出）
> 規格：`docs/art/ART_SPEC.md`（本輪新增第 1 節「剪影可讀性」三條）
> 逐批細節：`docs/changelog/ROUND29.md`；驗證證據：`docs/reviews/art-r29/`

## 一、先做的事：校正 R28 的自評

複審指出我在 R28 的三項判定過寬。逐項查證後**全部接受並已下修**（commit 355bc13）：

| Finding | R28 原判 | 校正後 | 我錯在哪 |
|---|---|---|---|
| ART-01 | 通過 | 部分通過 | 把「尺度階梯達標」當成整個 Finding 達標，但驗收條件是「無名稱輪廓辨認」。5 重建/17 微調/5 未動的事實原報告已誠實列出，**判定標籤卻寫「通過」——標籤與自己的紀錄不符** |
| ART-03 | 通過 | 部分通過 | 未驗收選角卡的換行行為 |
| ART-05 | 通過 | 大致通過 | 「出擊卡整合」不等於「高曝光介面整合」；`drawPortrait` 全案僅 1 個呼叫點 |

## 二、Finding 判定

| ID | 複審級別 | R29 判定 | 依據 |
|---|---|---|---|
| RE-01 角色輪廓 | P2 | **關閉** | 27/27 剪影互不撞形（22 名批次 A1＋5 名批次 A2）。根因：`outline()` 對 alpha>0 一律描邊，光暈被描進外輪廓 → 剪影是「發光半徑」不是身體 |
| RE-02 數字 token | P2 | **關閉** | `uiWrapText` 加 token 感知；387 條描述×3 欄寬實測 **舊斷 72 條 → 新 0 條**；9 個呼叫端從各自手寫的逐字元迴圈遷移 |
| RE-03 肖像整合 | P2 | **關閉** | 曝光面 1 → 3（個人頁主視覺、鎖定卡去飽和而非壓暗、NPC 對話框）；不重生肖像；fallback 無 layout shift |
| RE-04 面板密度 | P2 | **關閉** | 新增 `hub/layout.js` 三檔版型＋空狀態插槽（教學/進度/可按 CTA）；四面板×四解析度佔用率 **66–82%**，命中測試回歸 11/11 |
| RE-05 動態與 DPI 驗證 | P3 | **部分完成** | DPR2 與定位量測已做（見第三節）；**真人 5 人測試本環境做不到，未做也未代稱做過** |
| RE-06 場景 motif | P3 | **關閉** | `plainFloor` 六個固定邊緣像素與 desert v0 風紋改 seeded；室內動線上道具 8 → 0；六房焦點光皆由自己的設施領銜 |

## 三、獨立驗證揪出、並已修掉的兩個真缺陷

批次 C 只驗不改，發現我在 R28 判定「通過」的 ART-09 有實質漏洞：

1. **beam 所有權在紅綠色盲下失效**：boss `#ff5a3c` 與 event `#ffc23c` 在 protan/deutan 下**色相差 0.1°**，只剩線寬差；箭頭階梯被反鋸齒抹平（event 與 player 同為 8px）。我當初的三色覺驗收只測「光束對背景」，沒測「三族彼此可分」。
   → 批次 D 加**形狀通道**：boss 週期橫檔、event 25% 斷線＋空心箭頭、player 細直＋小箭鏃；achromat 遮罩（顏色不參與）下三族兩兩可分。`bm` 快照通道零變更。
2. **亮/冷生態玩家幾乎不可見**：desert 玩家標識對背景 WCAG **1.06**。根因量測顯示——改前**全場景「被標識畫暗的像素 = 0」**，標識只有加亮單一方向，遇到亮/冷地板必然失效。
   → 加暗通道（環的近黑描邊、beacon 真外框）＋形狀 tick；desert 暗筆畫 0→527px、p90 對比 4.33。beacon 由 N=4 懸崖改 N_SOFT=2 淡入，「密集但未被包圍」空窗的競爭視窗 −23~26%。

**架構師裁決**：接受批次 D 對「desert ≥3:1」判準的重新定義——量「標識筆畫對其實際背景」而非「48×48 patch 平均」。後者要達標必須用近乎不透明的暗塊蓋住玩家周圍，違背判準本意（讓玩家找到自己）。

## 四、上報使用者、本輪未動的事項

**`camera.zoom` 使用裝置像素**（`src/engine/renderer.js:34`）：`Math.round(Math.min(W/430, H/280))` 的 W/H 是 `cssW * dpr`，因此 DPR2 螢幕 zoom 3→5，**可視世界面積 +44%**（1920×1080 下 +78%）。這是**既有問題**（非 R28/R29 引入），但屬玩法公平性（共用排行榜），不在美術授權範圍，**已上報待裁決**。
同批擱置：`uiScale()` 2.6 上限使 1920×1080 DPR2 字級只有 DPR1 的 81.9%；`lw:1` 面板框線在所有 DPR 都是 2 device px 的半強度灰（102 處字面值）。

## 五、驗證狀態

- `npm run test:frontend`：**59/59**（每批次與合併後皆跑）
- co-op 三自測全過（beam 改動必跑；四種 boss move 的 `guestBeamsSeen` 全 true）
- registry：characters 27／enemies 63／icons 219，**missing 全為 `[]`**
- gameplay：def/stats 零變動；`bm` 快照通道零變更；`camera.zoom` 未動
- 5 個 commit，**未 push、未 merge**

## 六、誠實記錄

- **真人測試未做**：批次 C 明確聲明本環境無受試者與錄影管道，改用可重複的客觀量測（引擎給出玩家座標＋關閉識別美術的對照組＋5304 個滑窗排名），並提醒只該引用兩輪都成立的結論。替代量測**不能**推論反應時間或答對率。
- **動畫時間軸驗證未做**：工具鏈只能出靜態幀。
- **量測定義依賴**：面板佔用率 60–85%、標識對比等數字都依賴各自的量測定義（已寫在批次報告內），換定義數字會變；有疑慮時以截圖為準。
- **一次流程瑕疵**：我在批次 A2 的 commit 用了 `git add -A`，把批次 C 當時尚在產出的 95 個證據檔一併掃入。C 已在該 HEAD 重跑並保留兩輪對照，結論不受影響。
