# Soulshard Hunter — 第五輪改版報告（Round 5）

本輪把**初始大廳重建成一座多房間城鎮**，並完成你提出的 5 大項（含 7 個子項）需求；
同時用**多代理 Workflow 做了一輪對抗式程式碼稽核**，修掉了上一輪因斷線而留下的半成品 bug，
最後以「手動 pump + preview_eval」**跑了多場完整遊戲**（5 種生態 × 難度 1–5）驗證所有流程。

> 工程備註（延續第四輪的分工原則）：
> - **程式碼稽核**用 **Workflow**：11 個子系統 × 1 名審查代理 → 對抗式逐條驗證（共 19 名代理、~1.05M tokens）。
> - **完整遊戲測試**仍走專案既定的瀏覽器內 `preview_eval` 手動驅動（代理無法直接驅動 Canvas/Sprite），
>   以 AI 走位 + 手動 pump 跑滿 20 分鐘流程。
> - 上一輪斷線時，城鎮的**資料層大多已寫好**（NPC/公會/鍛造/衣帽店模組），但**整合層有破口**
>   （最關鍵的 `drawSortie` 整個漏掉，出擊面板一開就崩）。本輪先補完整合、再稽核、再測試。

---

## 第二批（Round 5b — 接續修正 + 你新增的 6～11 點）

> 上一批因 token 用盡再次中斷。本批接續完成，並修掉兩個你回報的關鍵 bug。同樣以 **Workflow 稽核**
> （`round5b-audit`：8 子系統 × 10 代理 → **0 個確認 bug**）＋ **瀏覽器內完整跑圖**（5 生態 × 難度 1–5 全通關、零例外）驗證。

| # | 需求 | 實作 | 狀態 |
| --- | --- | --- | --- |
| 3 / 9 | **前六支角色仍是初版造型（只換色）** | **根因：載入順序烘焙 bug。** `state.js` 很早就（透過 import 鏈）拉進 `characters.js`，它**立刻烘焙**角色 sprite——但此時 `art/heroes.js` 尚未執行、`HERO_ART` 還是空的，於是 6 名核心英雄全部退回通用兜帽身體（看起來只換色）。修法：在 `characters.js` 頂端 `import '../../art/heroes.js'`，**保證體型原型先註冊再烘焙**。現在 6 名核心英雄各自是獨特剪影（騎士帶劍盾、炎法戴火冠、遊俠持弓…）。 | ✅ 驗證 |
| 7 | **鐵匠鋪只能升級魂晶彈、其餘選不到** | **根因：命中測試 bug。** 武器列每列把寬度存在 `r.wd`（因為 `r.w` 放的是武器物件），但 `inside()` 讀 `r.w` 當寬度 → `r.x + 物件` = `NaN` → 任何一列都點不到，只剩預設選中的魂晶彈。修法：`updateForge` 改用 `r.wd` 做命中測試。現在每把武器都能選取鍛造。 | ✅ 驗證 |
| 11 | **怪物太少、血傷太低 → 要高難度大量怪、被追逐感** | 重調 `balance.js`＋`run.js`：同屏上限 `SPAWN_CAP_BASE 7→14`、`/威脅 4.8→7`、`上限 115→260`，生成間隔 `2.0/0.55→1.25/0.30`，每波 `2 + 威脅/1.5`、同時 2–4 種怪；怪血 `1.15→1.45`、怪傷 `1.0→1.08`（**威脅來自數量**，每擊傷害保持 D1 可玩，D2–D5 由 `diffMul` 加碼）；`EARLY_GRACE 110→140` 緩開局。實測單局密度 **38→230**（峰值 ~231，逼近上限），「被一大群追著跑」成形。 | ✅ 驗證 |
| 8 | **局外花錢前要詢問 + 各功能可重置** | 新增**購買確認彈窗**（顯示花費＋目前金幣，確定/取消），攔截天賦/鍛造/設施/造型/重新進貨所有花費；每個分頁右上有**重置鈕**（天賦/鍛造/設施各自獨立），重置時**返還已花費金幣**並二次確認。 | ✅ 驗證 |
| 10 | **衣帽店分兩頁 + 30 分鐘隨機刷 + 隱藏造型** | 衣帽店拆成兩分頁：**我的造型**（橫列每個英雄＋其已擁有造型 chip，點即換）與**造型商店**（4 格、**每 30 分鐘真實時間**自動換新＋付費重抽、顯示倒數）。新增 **4 款隱藏造型**（魂晶巨像/虛無亡魂/機甲獵手/熾天使），用 `body(p,f)` **整個重畫身體**——和初始兜帽完全不同的剪影，商店約 45% 機率刷出、標「★隱藏」。 | ✅ 驗證 |
| 6 | **大廳配色單一平板、大門偏移** | 每個房間鋪上**專屬色調地板暈染**（教堂藍/公會金/鐵匠橙/衣帽紫…），region 一眼可辨、不再一片灰；廣場**四個入口都補上對齊門洞的拱門**（原本只有上下兩個且偏移）。 | ✅ 驗證 |
| 2 | **成就再擴充、加解鎖掛鉤** | 再加一組 R5B：**生態精通 / 百戰**家族＋ 7 個解鎖型（虛空斗篷、彩晶核心、淨化波、時滯爆發、聚能雷射、連鎖閃電、黑洞）＋ 2 個隱藏目標。成就總數 **→ 183**。 | ✅ 驗證 |

### 第二批稽核與測試
- **`round5b-audit` Workflow**：char-art 載入順序 / 衣帽店雙頁 / 隱藏造型 / 確認與重置數學 / 鍛造命中修正 / 群湧平衡 / 成就解鎖 ID / 跨模組整合 — **8 子系統全綠，0 個確認 bug**（誤報如「重複解鎖來源」於驗證階段駁回）。
- **完整流程**：5 生態 × 難度 1–5 各跑滿 20 分鐘——小王 5/10/15 分、終王 20:00、通關解鎖、死神 +30s、結算入帳，**全數通關、零 JS 例外**；峰值同屏 ~231 隻仍穩定。
- **全新存檔開局**：無錯誤、所有面板與 NPC 對話皆正常。

---

## 逐項需求對照（你的 1–5）

| # | 需求 | 實作 | 狀態 |
| --- | --- | --- | --- |
| 1 | **B 鍵商店**與隱藏模式（↑↑↓↓←→←→BA）衝突 | Konami 末段的 `B` 由 `cheats.js` 在**命中前綴 `↑↑↓↓←→←→B` 當下**設 `Cheats.eatShop`，`run.js` 的商店判定（`pressed('shop')`）看到旗標就**吞掉這一次商店切換**並清旗標；一般情況下按 B 仍照常開商店。稽核確認邏輯正確、無誤觸。 | ✅ 驗證 |
| 2 | **成就**擴充種類、並與解鎖武器/道具/關卡掛鉤 | 成就總數提升到 **150+**：除原家族外新增 **修為/無瑕/共鳴(羈絆)/鍛魂(鍛造)/人脈(NPC)/聲望(公會)/時尚(造型)/群英之師** 等家族，加上**每生態 2 項通關成就**與隱藏目標；另有 **~25 個解鎖型成就**直接解鎖武器（光環刃、散鏢扇、黑洞、聚能雷射…）、被動（過載、處決、戰旗…）、裝備（龍鱗甲、星墜法球…）與道具。 | ✅ 驗證 |
| 3 | **角色造型**：有些角色仍是第一版；造型坊額外造型只換色、缺乏獨特性 | (a) **全 21 名英雄**都對應 `art/heroes.js` 的**獨特體型原型**（18 種：騎士/聖騎/法師/炎法/術士/死靈/遊俠/獵手/盜賊/武士/狂戰/槍手/武僧/薩滿/女武神/斥候/風暴祭司/虛空法師），`HERO_MAP` 把重複原型拆開，**沒有兩個英雄是換色雙胞胎**。(b) 造型（SKINS）除了重新上色，**每套都加上專屬配飾 `deco`**（火焰冠、水之光環、葉冠、金冠、虛空雙角、冰刺冠、幽靈光環、碧玉寶石、緋紅犄角…），不再只是換色。 | ✅ 驗證 |
| 4 | **包圍事件**：怪量提升、必殺才出去、行動緩慢 → 應為「圓圈都是怪在靠近角色，要打死一兩隻或撞牆才能出去」 | 「**魂牢**」重做：一圈（10+）怪在你身邊**主動收攏**（`SURROUND_SPEED_MULT 0.85`、`lockR` 以 `SURROUND_CLOSE_SPEED` 持續內縮），並用**結界鎖把你夾在圈內**（`SURROUND_MUST_CLEAR`）；**殺出 2 隻（`SURROUND_BREACH_KILLS`）或撞牆**（`SURROUND_PROBES` 偵測）即破圈逃出，另有 `SURROUND_LIFE` 安全逾時防卡死。 | ✅ 驗證 |
| 5 | **初始大廳美化** | 大廳由單畫面改造成 **3×3 多房間城鎮**（`world.js makeCamp`：教堂/公會/鐵匠鋪/衣帽店/成就殿堂/個人小屋 環繞中央廣場，另有花園/市集兩個風味房），房間以門洞相連，各房擺設專屬裝飾。 | ✅ 驗證 |
| 5-1 | 大量 **NPC**（可對話／劇情／接任務／詢問遊戲要素） | `content/npcs.js`：**10 名居民**（神官莉雅、公會長鐵爺、前台小鈴、鐵匠戈登、裁縫薇拉、殿堂管理員奧德、嚮導蕾恩、雜貨商老潘、退役老兵賈克、孩子小米），每人有招呼語＋多個可問主題；**部分台詞會依存檔動態變化**（你的通關數/解鎖數/聲望階級）；「管理員」型 NPC 對話結束會**直接帶你進該建築的面板**。 | ✅ 驗證 |
| 5-2 | **成就殿堂**設為房間展示 | 成就殿堂為獨立房間（金色旗幟＋立柱），互動開啟**成就殿堂面板**（雙欄、捲動、顯示解鎖獎勵）。 | ✅ 驗證 |
| 5-3 | **任務公會**搬到公會區（公會長／前台／任務板＋累積公會經驗換獎勵） | 公會房有公會長＋前台＋任務板；面板分**任務委託 / 公會等級**兩頁。`content/guild.js`：每次出擊與領取委託累積**聲望**，跨階即可**領取一次性獎勵**（金幣＋武器/被動/裝備/道具），共 12 階。 | ✅ 驗證 |
| 5-4 | **天賦**改為教堂（與神職人員/女神像互動調天賦） | 教堂房有神官＋女神像；與女神像（或神官對話結束）開啟**天賦面板**。＊修掉了神官 `station` 設成 `church` 導致開不了面板的 bug（見下）。 | ✅ 驗證 |
| 5-5 | **設施工坊**做成鐵匠鋪（鍛鐵：武器局外升級＋1~3 特效＋原能力增強） | 鐵匠鋪面板分**鍛造 / 營地設施**兩頁。`content/forge.js`：花金幣**永久強化某把武器**——鍛造等級（每級傷害 +8%，上限 5）＋**最多 3 種鑲嵌特效**（銳利/貫穿/巨力/廣域/疾速）。局內 `player.js` 只在**該武器開火時**套用其鍛造修正再還原，跨武器互不干擾。 | ✅ 驗證 |
| 5-6 | **造型工坊**做成衣帽店（隨機刷 1~4 個造型可買，套用於角色，出擊前設定） | 衣帽店每次歸來上架 **1~4 個隨機造型**（`content/skinshop.js`），可**重新進貨**（70 金）；買下後**套用在當前英雄**，出擊前於出擊面板選角即生效。造型仍依角色分別擁有（`charId:skinId`）。 | ✅ 驗證 |
| 5-7 | **個人小屋**（原本純擺設） | 個人小屋面板現在展示**生涯戰績**（出擊/擊殺/通關/屠王/弒神/最佳分數/最長存活/最高威脅/最高等級/無傷通關/鍛造次數/結識居民）與**收藏進度**（英雄/武器/被動/裝備/造型/成就）。 | ✅ 驗證 |

---

## 多代理 Workflow 程式碼稽核（11 子系統 / 19 代理）

對整個第五輪變更做了一輪**對抗式稽核**（每個子系統先審查、findings 再交由獨立代理逐條反駁驗證）。
結果：**5 個 bug 經驗證為真並修復**，其餘子系統（衣帽店、成就/解鎖、Konami/商店、美術 sprite、城鎮地圖）判定健康；
數個誤報（如「facility icon 不存在會崩」）因為有 `iconOr` 後備與 magenta 佔位而被驗證階段正確駁回。

### 修復清單

| 嚴重度 | 檔案 | 問題 | 修法 |
| --- | --- | --- | --- |
| **crit** | `scenes/hub.js` | **`drawSortie` 整個方法漏寫**（上一輪斷線殘留）：`render()` 呼叫它 → 一開「出擊」面板就 `TypeError` 崩潰。出擊是核心畫面。 | 補回 `drawSortie()`（選角卡＋生態＋難度＋出擊鈕），與既有 `sortieLayout/updateSortie` 對齊。 |
| **high** | `player.js:61` | 武器**進化**（checkEvolve）後新實例沒帶 `forge`，鍛造的等級/特效**全部失效**。 | 進化實例繼承母武器的 `forge: inst.forge`。 |
| **high** | `player.js:78` | 武器**融合**（fuseWeapons）同上問題。 | 融合實例繼承 `forge: target.forge`。 |
| **high** | `content/npcs.js:15` | 神官 NPC `station:'church'`，但面板 ID 是 `'talents'` → 對話結束開不了天賦面板（正是 5-4）。 | 改 `station:'talents'`。 |
| **med** | `state.js:75` | 舊存檔若 `skinShop.roll` 為非數字，`skinshop.js` 的 `roll+1` 會字串相連污染存檔。 | 遷移時補 `if (typeof roll!=='number') roll=0`（比照 `guild.xp`）。 |
| **med** | `scenes/run.js:558-572` | 魂牢破圈提示訊息用當幀的區域變數 `wall`，玩家撞牆後離開牆面，破圈訊息會顯示成「殺出缺口」而非「貼牆突圍」。 | 破圈當下記錄 `sur.wasWall`，結束時用它選訊息。 |

---

## 完整遊戲測試（多場、跑滿 20 分鐘流程）

以瀏覽器內手動 pump 跨**5 種生態 × 難度 1–5**各跑一場完整 20 分鐘：

| 生態 | 難度 | 小王 5/10/15 分 | 終王 20:00 | 通關解鎖 | 死神 +30s | 結果 |
| --- | --- | --- | --- | --- | --- | --- |
| 水晶洞窟 cavern | 1 | ✓ | ✓ | ✓(→3) | ✓ 斬殺 | 通關 score 13342 |
| 幽影地穴 crypt | 2 | ✓✓✓ | ✓ | ✓ | ✓ 斬殺 | 通關 score 14434 |
| 霜寒冰原 frost | 3 | ✓ | ✓ | ✓ | ✓ 斬殺 | 通關 score 14014 |
| 熔岩深淵 inferno | 4 | ✓✓✓ | ✓ | ✓ | ✓ 斬殺 | 通關 score 14518 |
| 虛空裂界 void | 5 | ✓✓✓ | ✓ | ✓ | ✓ 斬殺 | 通關 score 14758 |

**全程零 JS 例外。** 另外驗證：

- **實戰 AI 連打 4 分鐘**（真實走位＋自動瞄準＋升級三選一）：等級 11、262 擊殺、6 把武器（含升級）、4 被動、**觸發羈絆 `bond_arsenal`**、傷害歸戶 7 個來源——零錯誤。
- **大廳互動全綠**：買天賦 / 鍛造等級＋特效 / 升級設施 / 領取公會階級獎勵（+150 金）/ 衣帽店買並套用造型——金幣增減與狀態變更皆正確。
- **魂牢（task 4）實測**：圈住 11 隻，玩家試圖逃 1000px 被夾在 `lockR≈223px`；殺 1 隻仍被夾、殺 2 隻破圈可逃；**撞牆**亦能破圈（`wasWall` 正確）。
- **武器進化保留鍛造**（task 3/5-5 修復後）：`w_soulbolt→w_soulstorm` 後 `forge` 仍在。
- **全新存檔開局**：無錯誤、預設值正確、所有面板可開、所有 NPC 對話（含動態台詞）在空存檔上不丟例外。

---

## 重點檔案

```
src/game/scenes/hub.js          多房間城鎮場景 + 所有面板（補回 drawSortie）
src/game/world.js  makeCamp()    3x3 城鎮地圖（門洞/房間錨點/裝飾）
src/game/content/npcs.js         5-1 城鎮 NPC + 對話
src/game/content/guild.js        5-3 公會聲望階級 + 獎勵
src/game/content/forge.js        5-5 武器局外鍛造（等級 + 特效）；player.js 套用
src/game/content/skinshop.js     5-6 衣帽店輪替上架
src/game/content/achievements.js task-2 成就家族大幅擴充 + 解鎖掛鉤
src/art/heroes.js                task-3 全 21 英雄獨特體型映射
src/art/town_*.js                城鎮 NPC/教堂/公會鐵匠/裝飾/個人房 程序美術
src/game/balance.js  SURROUND_*  task-4 魂牢數值
src/game/cheats.js   eatShop     task-1 Konami 末段 B 不誤開商店
```
