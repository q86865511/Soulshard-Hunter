# 魂晶獵手 · Soulshard Hunter

一款以原生 HTML5 Canvas + ES Modules 製作的 roguelike 像素生存遊戲（Vampire-Survivors 風格）。
**所有美術都是程式即時生成的像素畫（無外部素材）；音效為 WebAudio 合成，配樂為 12 首實際錄製曲目**（`assets/music/`，依遊戲狀態自動切換、合成器為後備）。可純單機離線遊玩，**也可選擇性連上雲端後端**（帳號 · 跨裝置雲端存檔 · 共享排行榜）。

**只需走位、武器自動攻擊**；探索多種生態大地圖、敵人不斷湧出，升級選武器與被動、滿級進化合成；帶回的金幣在城鎮解鎖角色、天賦與設施。

## 執行方式

不需安裝套件，只要一個靜態伺服器（ES Modules）。專案附了一個無快取的 Node 伺服器：

```bash
# 在專案根目錄
node tools/serve.mjs
# 然後瀏覽器開 http://localhost:5173
```

（開發時也可用 Claude 預覽工具，設定已在 `.claude/launch.json`。）

## 操作

| 按鍵 | 動作 |
| --- | --- |
| `WASD` / 方向鍵 | 移動（武器自動瞄準開火，無需手動攻擊） |
| `Shift` | 衝刺（短暫無敵） |
| `1` / `2` / `3` | 升級時選擇強化卡片（地面道具**撿起即用**，已無道具欄） |
| `B` | 隨時開啟**魂晶／鐵砧商店**（不再綁在地圖上；鐵砧三選一顯示替換前後差異） |
| `E` | 互動（祝福神龕 / 城鎮站點 / NPC：水井·迷途之魂·魂晶礦脈·流浪鐵匠 / 通關後離場） |
| `Tab` | 查看當前 build（武器 / 被動 / 裝備 / **羈絆** / 數值；圖示可懸停看說明） |
| `M` | 半透明**放大地圖**浮現於畫面中央（再按關閉） |
| `Esc` | 暫停選單 / 設定（音量 / 震動 / 靜音 / 重置存檔） |
| 城鎮：`1` 天賦 / `2` 設施 / `3` 成就 / `4` 任務 / `空白` 出擊（選角色/關卡/難度） |

也可把滑鼠移到 HUD 或 build 頁的武器 / 被動 / 裝備圖示上查看說明。每位角色可獲取的武器/被動有上限（**武器數 6、被動 14；武器等級上限 7**，角色等級則無上限）。

## 遊戲循環

1. **城鎮（Hub）**：可走動的營地。用金幣在「天賦祭壇」買 4 系永久天賦、在「設施工坊」蓋設施、查看「成就殿堂」與「故事任務」（領取章節獎勵）；走向「傳送門」開啟出擊面板：**選擇角色（分頁/滾輪）、關卡、難度**後出擊（角色與更高難度用金幣或達成成就/破關解鎖，含隱藏英雄）。
2. **單一關卡（局內）**：一局＝一張**超大生態地圖（138×102）、限時 20 分鐘**。**威脅等級隨時間爬升**（1→約13，影響怪物階級與強度），**難度**再乘上整體強度；同一時間只出 **1~3 種怪物**並隨時間輪替（**遠程怪減少、近戰增加、移速隨時間變快**）；地圖散布**陷阱地形**（熔岩 / 尖刺 / 毒沼 / 荊棘，傷害已提高，對雙方皆有效）與**互動 NPC**（祈願水井 / 迷途之魂）。每 **5 分鐘**降臨一隻**各不相同的小王**；期間有**特殊事件**（提摩蘑菇、**怪物包圍縮圈**、**希格斯持續炸彈雨**）與**特殊怪物**。怪物 / 技能帶**異常狀態**（緩速・流血・灼燒・中毒・暈眩・擊飛）。
3. **最終決戰與死神**：第 **20 分鐘**該關**最終首領**降臨；**擊敗即破關** → 解鎖**下一關卡**與**本關更高難度**，並給予大量金幣。破關 **30 秒**後，隱藏的**死神**降臨——**可被擊殺**，斬殺另給傳說獎勵；亦可按 `E` 直接離場通關。
4. 升級三選一：**新武器 / 武器升級 / 被動**（卡片依稀有度上色並標示數值）。武器滿級（Lv.7）且滿足條件可**進化合成**——畫面會提示「可合成」但不公開公式（**2 把滿級武器，或 1 把滿級武器 + 一個被動**）。**詛咒強化**提供強力效果但附帶負面代價。走到**魂晶商店**買史詩/稜彩裝備與**能力值鐵砧（購入後三選一）**；整局不取裝備、專買鐵砧者，有機會觸發一次性**稜彩祝福**。
5. **死亡 / 破關**：結算分數與排行榜，帶回金幣存入金庫用於永久成長、解鎖成就與推進故事。

## 內容規模

核心內容 + 多輪 **dynamic workflow（多代理工作流，生成→審查兩階段）並行生成**，全部含程序化像素美術：

| 類別 | 數量 |
| --- | --- |
| 敵人（含 **8 名多階段首領** + 隱藏**死神** + 特殊怪物） | **48** |
| 自動武器（含進化合成） | **30** |
| 可玩角色（含隱藏英雄） | **21** |
| 被動能力（含 **詛咒強化** + 異常狀態被動） | **53** |
| 裝備（含史詩/稜彩） | **40** |
| 道具（含能力值鐵砧） | **28** |
| 天賦 / 設施 | 20 / 11 |
| 生態 tileset | 5 |
| 成就 / 故事章節 | **190+ / 15** |
| 羈絆 · 角色專屬武器 · 贊助者事件 | **13 · 8 · 16** |
| 角色造型（含 **4 款隱藏全身造型**） | **15** |
| 一般 / 隱藏任務 | **6 / 3** |
| 錄製配樂曲目 | **12** |

## 系統

- 走位自動攻擊、多武器同時開火、武器升級（上限 Lv.7）/ 進化合成（**2 滿級武器，或 1 滿級武器 + 被動**，隱藏公式）；攻速由慢到快隨局內時間提升
- 多角色（被動 + 起始武器 + 解鎖條件）、隱藏英雄
- **20 分鐘時間軸**：每 5 分鐘各異**小王**（擊殺後掉落**競技場式事件三選一**：增益 / 風險取捨 / 犧牲交易 / 限時挑戰小任務）、20 分**最終首領**、破關 +30 秒可擊殺的隱藏**死神**（終極 DPS 試煉，需大量堆疊才打得贏）；時間驅動威脅等級、**難度分級**、**破關解鎖下一關與更高難度**
- **結算畫面**：呈現本局武器（含等級）/ 被動 / 裝備與其**數值效果**（傷害、射速、吸血…），並列出本局**解鎖的成就與內容**
- **異常狀態系統**（緩速 / 流血 / 灼燒 / 中毒 / 暈眩 / 擊飛）；玩家武器與被動可附加，主題怪 / 首領亦會施加；首領階段轉換會擊飛玩家
- **怪物包圍縮圈**（厚血怪收攏，需清缺口突圍）、**希格斯持續炸彈雨**、提摩蘑菇、陷阱地形（傷害提高）、**互動 NPC**
- **詛咒系統**：強力被動附帶負面代價；卡片依稀有度上色並標示數值
- **局內魂晶商店**：**裝備鐵砧（史詩/稜彩三選一）** 與 **能力值鐵砧（三選一）** 兩種隨機鐵砧，與隱藏「純能力值」一次性稜彩祝福；**撿裝備跳出分類選單、地面道具撿起即用**
- 城鎮永久成長（天賦樹 + 設施）、可互動的**任務公會（接取一般/隱藏任務、點選追蹤）/ 成就殿堂 / 造型坊（購買角色替換造型）建築**、**主線任務常駐螢幕左側**、**成就解鎖內容**、**開場故事過場**、選角頁顯示英雄能力差異、金幣經濟
- **錄製配樂**（12 首：標題 / 大廳 / 各生態 / 小王 / 最終首領 / 死神 / 結算 / 死亡，串流播放＋交叉淡入；任一檔缺失自動降級為程序合成音樂）、可調震動（瀕死才劇烈）
- **120 Hz 模擬**（低輸入延遲）、設定選單、排行榜、**小地圖（移至血量/衝刺 UI 下方、放大，與大地圖圖示一致）**、玩家**狀態列**、HUD 提示與 build 檢視頁
- 所有難度 / 經濟 / 節奏數值集中於 `src/game/balance.js`，便於微調（**第六輪整體調硬**：補上半數武器漏掉的全域減傷、暴擊上限、meta 數值下修、怪物/首領更痛、收緊回復與經濟；難度集中於中後期與 meta 天花板，開局維持可玩）

## 線上多人（雲端 · Phase 1）

可**選擇性**啟用，不影響單機離線遊玩（未登入/連不上即自動退回本機 `localStorage`）。

- **帳號 · 跨裝置雲端存檔 · 共享排行榜**：右下角帳號列可登入/註冊；存檔自動同步上雲（防抖），完局成績上傳排行榜。
- **伺服器權威計分**：排行榜分數由伺服器以 `kills/stage/time/difficulty/reaper` **重算**（忽略客戶端宣稱值），防作弊。
- 後端在 `server/`（Node + Fastify + PostgreSQL；JWT + bcryptjs + zod）。本機可一鍵起：

```bash
cd server
JWT_SECRET=$(node -e "console.log(require('crypto').randomBytes(48).toString('hex'))") docker compose up --build
# 或無 Postgres 的純前端測試：  npm install && npm run dev:fakedb   （記憶體後端，重啟即清空）
node test/smoke.mjs   # 後端端對端煙霧測試（18/18）
```

- 這是 [`docs/MULTIPLAYER_PLAN.md`](docs/MULTIPLAYER_PLAN.md) 的 **Phase 1（雲端地基）**；即時合作同屏為 Phase 2（尚未實作）。
- **部署到 Oracle Cloud** 的完整步驟見 [`docs/DEPLOY_ORACLE.md`](docs/DEPLOY_ORACLE.md)。

## 架構

```
index.html                      Canvas + 載入畫面 + 全域錯誤攔截
src/
  main.js                       啟動：接線引擎/內容/場景，主迴圈
  engine/                       與遊戲無關的引擎層
    loop.js  math.js  input.js  renderer.js(相機/世界↔螢幕/UI)
    palette.js  sprites.js(程序化像素繪圖 API + 註冊表)
    particles.js  audio.js(WebAudio 合成音效/音樂)
  art/                          程序化美術（sprite 定義）
    core.js  icons.js  content_icons.js  hub.js
    gen/                        ← workflow 生成的美術（自動整合）
  game/
    state.js                    存檔/局外 META、局內 run 狀態、數值
    scene.js  scenes/           場景：title / hub / run（+ refs 解耦）
    world.js  floor.js          地城世界（磚塊碰撞/實體/戰鬥）與樓層生成
    player.js  enemy.js  projectile.js  pickup.js
    hud.js
    content/
      registry.js               ← 所有內容的註冊表（整合接縫）
      enemies/abilities/items/equipment/talents/facilities.js  核心內容
      gen/                      ← workflow 生成的內容（自動整合）
tools/integrate.mjs             把 workflow 輸出（JSON）寫成可載入的檔案
```

### 如何擴充內容

內容與美術完全資料驅動：

- **新敵人**：在 `src/art/...` 用 `defineAnim(name, w, h, frames, draw)` 畫 sprite，再到 `Enemies.register({ id, sprite, ai, hp, ... })`。AI 可選 `chase/flyer/shooter/charger/wander`。
- **新能力/道具/裝備/天賦/設施**：`Xxx.register({...})` 並用 `defineIcon('ability_'+id, bg, draw)` 等慣例命名圖示。

像素繪圖 API（`Painter`）：`px/rect/line/ellipse/ring/mirrorX/outline(...)` + 共用調色盤 `P` 與 `sym` 符號庫，確保風格一致。
