# Bot 成長診斷與後續單一變因實驗
建立：2026-09-16。基準：R31 合併 commit 61cc70b；A 為原策略，B 為已否決候選。

## D1 觀測，不改遊戲
只修改 tools/bot/ 與文件；不改 src/、遊戲數值、協定、存檔。診斷預設關閉，明確 --diagnostics growth-v1 才啟用，且必須有 --strategy 與 manifest。
量測函式包裝必須保留 this、參數、回傳值、例外及原呼叫次數；不使用 Math.random、不推進額外遊戲 tick、不修改策略決策。
驗收：純函式／包裝測試先紅後綠；真 Chromium 驗證診斷前後遊戲語義；原回歸保持通過。

## D2 量測
記錄 XP 產生、拾取、逾期、其他移除、場上剩餘；拾取延遲分布；XP 入帳、升級時間與間隔；每次升級面板的供給與實際選擇、持有武器等級。
每 30 模擬秒記錄 kills、XP、level、剩餘 XP、武器／被動數；每秒取樣移動情境中的拾取安全閘、可見 XP 與其他拾取物、朝向 XP 的程度。
驗收：XP 拾取物價值守恆；入帳與拾取分開計數；多次升級與非 gainXp 的 XP 變化不混淆；資料包含截斷／缺漏指示且驗算拒絕不完整診斷。

## D3 診斷實跑
A/B 各 10 生態 × 27 角色 × diff1 × 1 局＝270，共 540；策略 seed=1，parallel=4，完整局（上限 1350 模擬秒），依生態 AB/BA 交替。
新目錄、固定程式來源、全部原始失敗保留；不改寫 R30/R31 資料。按同參數續跑；啟動失敗、外部程序占 5173 或連續兩次無法續跑時停下詢問。
報告同時比較全局與共同早期窗口（含在險局數），避免只比較存活者。分開說明 XP 供給、收集效率、升級與選擇的證據，區分觀測關聯與因果。
驗收：540 唯一 key、0 最終 error、diagnostic schema／守恆通過；獨立計數與檔案指紋核對。

## D4 下一個實驗
先依 D3 診斷選出一個具體、單一變因候選 C，寫明機制假說與與 A 的唯一差異，再固定程式與規格後實跑，不能邊看確認結果邊改策略。
保留 A，先導 A/C 共 540；確認 A/C 共 8100（每策略 10×27×5×3 seed）。
沿用固定採用門檻：有效局通關率至少 +1pp、獨立分層 bootstrap 95% CI 下界 >0、全局通關率同方向、timeout/error 排除率不得升高。
遊戲 RNG 不受控，不把相同策略 seed 當相同世界；先導、診斷與確認資料分開。未通過仍保留 A。
驗收：完整資料、選擇／策略分派證據、CI、明確採用判定，不能以只有工具實作完成代替實驗結果。

## D5 其餘目標界線
真人評估：目前使用者確認沒有真人資料。先準備實際可執行的流程、表單與分析規則；未取得資料前評估結論保持未完成，不捏造受測者或真人行為。
架構升級：原禁止修改 src/、協定與存檔的限制仍有效，具體範圍等待裁決；先做現況與風險盤點。
本輪不自動 push／PR／merge／部署。
