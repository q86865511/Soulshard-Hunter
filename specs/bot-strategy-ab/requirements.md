# bot-strategy-ab — 需求
日期：2026-09-15；使用者已授權實作、540 局先導與 8100 局確認實驗。

## R1 單一變因
預設／A 完全保留 R30 R4。明確指定 B 時，僅把一般被動與新武器的優先序交換：
進化配對 ＞ fuse ＞ 持有武器升級 ＞ 一般被動 ＞ 新武器 ＞ 其他。
滿槽、滿級、進化與 fuse 保護、signature、走位、seed tie-break、面板處理不變。原 R30 規格及資料保留。
驗收：同時可選一般被動與新武器時 A 選新武器、B 選被動；高階優先與保護在 A/B 皆成立；未知策略拒絕。原測試不放寬。

## R2 資料隔離
CLI --strategy A|B；每個策略／seed／生態使用獨立目錄。明確策略的 manifest 記錄策略、完整 commit SHA、執行來源 SHA256、toolVersion、gameVersion、參數。
識別不符或無 manifest 的既有紀錄拒絕續跑；不指定策略也不得寫入實驗目錄。每行帶策略和實際選擇稽核計數。
驗收：策略、seed、commit、來源、參數漂移皆拒絕；同參數續跑只補 error／缺漏；中段壞 JSON 拒絕；原始失敗保留，最終結果用 dedupeLatest。
每次執行記錄起訖、wallMs、exitCode、api_hits、browser_restarts。輸出原始資料與 manifest gitignored；摘要入版控。

## R3 先導
10 生態 × 27 角色 × diff1 × 1 局 × 2 策略＝540 唯一排程 key，seed=1。
依生態交替 AB／BA，parallel=4，一次只有一個 runner 佔 5173。
驗收：兩策略各 270，無缺漏／額外／最終 error；schema、策略稽核、計數器與報告完整。據實測耗時估確認階段；先導不參與確認實驗效果估計。

## R4 確認與判定
每策略 10 × 27 × 5 局 × 3 seed（1,2,3）＝4050；兩策略 8100。新目錄，不重用先導。
遊戲 RNG 不受控，各組為獨立抽樣；seed 是策略 seed，不是遊戲種子。
固定主要判準：B 的 clear/有效局 − A 至少 +1pp，分層獨立 bootstrap 95% CI 下界 >0；clear/全局同方向，且 B 的 timeout+error 比例不得高於 A。未達則保留預設 A（不代表證明等效）。
有效局定義沿用 R30：排除 timeout/error；另報全局分母。報告整體／逐生態兩種通關率、time p25/p50/p75、level、abilities 空值、endReason、錯誤／重試、實測耗時與重開。
驗收：獨立驗算 8100 key；統計測試含已知差值、退化／空值與分層資料；決策摘要列判準及數字、限制與下一步。明寫「系統性質非真人行為」「遊戲 RNG 不受控」「策略等級」，不得聲稱真人通關率。

## R5 不變條件與驗證
不改 src/、遊戲數值／協定／存檔；每局 context 隔離、API abort、rAF stub 與 dt=1/120 沿用。
本機原 127 單元、27 整合、server check＋120＋65、frontend 59 加新測試全綠。整合與前端測試在批次停止且 5173 空閒後跑。
Chromium 無法啟動、5173 為外部程序、連續兩次續跑失敗即停下詢問。不得重新建目錄繞過失敗。
不部署；本次目標不包含後續 push／PR／merge。

## 執行後補記：歷史 session 計數缺漏（2026-09-16）
本次有 1 個舊版開局前失敗 session 的 API／重開計數未輸出。限制保留於摘要，不回填或刪除歷史證據。分析以 null、缺失數與已知下界呈現；不放寬成功 session、策略識別、完整 key 或最終 error=0 的檢查。後續版本已補上失敗退出計數與回歸測試。此補記只說明歷史可觀測性限制，R1 策略差異、R3/R4 樣本數及預先採用門檻均未改動。
