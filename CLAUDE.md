# CLAUDE.md — Soulshard Hunter (魂晶獵手)

Dev guide for working on this repo. Read this before editing. Keep it concise — **per-round detail lives ONLY in `docs/changelog/ROUNDx.md`** (the single home for version notes); this file describes the *current* architecture + the traps that bite.

## What it is
A single-player (optionally cloud-connected) Vampire-Survivors-style pixel roguelike. **Vanilla HTML5 Canvas + ES Modules, no build step**; the only external assets are the **recorded MP3 soundtrack** in `assets/music/`. All pixel art is procedurally generated in code; SFX are WebAudio-synthesised; music is the recorded soundtrack (state-mapped + crossfaded) with a procedural-synth fallback. Walk-only control — weapons auto-target and fire.

## Current state (rounds 1–21 done; public version **V2.0**)
A run = one biome, **20 min**, at a chosen difficulty (or the **無盡/每日/週常** modes). Threat ramps 1→~13; distinct mini-bosses at 5/10/15 min; biome final boss at 20:00 → clear unlocks the next biome + difficulty; a killable Reaper descends 30 s later.

**Content registry** (counts verified live via `__DBG.reg()`): **27 heroes · 43 weapons** (incl. evolved) **· 54 passives · 28 items · 63 enemies** (incl. 10 biome final bosses + R18 mobs + 4 R20 event mobs + boss_pillar) **· 60 equipment · 27 exclusive weapons** (every hero has one, as of R21) **· 16 skins** (5 hidden full-body) **· 12 bonds · 16 patron events · 20 talents · 11 facilities**, plus **219 achievements** (many gate unlocks), **19 story chapters**, and a **12-rank guild reputation track**. All **10 biomes** have a dedicated final boss.

**Systems in place:** status effects · bonds/synergies · character-exclusive weapons · unique per-hero bodies · damage attribution + results ranking · B-key shop (before/after equip diffs) · 4-tier rarity · soul-jail must-clear surround · anti-AFK drain · achievement-gated unlocks · **multi-map ruin-town hub** (out-of-run forge / talents / facilities / guild quests / wardrobe / 魂晶銀行 / NPC 好感度 / pets / decor) · **cloud backend** (accounts · cloud save · leaderboards · admin + moderation suite) · **real-time co-op** (host-authoritative relay + friends/lobby) · **無盡 + 每日挑戰 + 週常懸賞** modes · **data-driven boss moves** + in-run events. Topic-by-topic detail is in **Key subsystems** below.

**Round index** (one line each; full notes in `docs/changelog/`):
R1–R3 core loop/content/balance (`OVERHAUL.md`) · R4 bonds/exclusives/unique bodies/patron events/damage attribution · R5/5b town hub/forge/guild/skin shop/denser swarm · R6 recorded soundtrack/cloud Phase 1/harder balance · R7 real-time co-op (host-authoritative relay) · R8 57-agent bug/UX audit · R9 art glow-up + 5 new biomes (→10) · R10 co-op resilience (migration/reconnect/spectate/score authority) · R11 keybinds/返回大廳/hub dev panel · R12 訪客排行榜/房間化地圖+隱藏房 · R13/R14 admin → moderation suite · R15 account-UX overhaul · R16 UX + 後台強化 (feedback/SW/7-tab admin) · R17 wardrobe rework + 4-tier rarity + hidden-room exclusives + economy · R18 g_* heroes playable (→21)/5 R9-biome bosses/endless curses/daily+weekly/NPC好感度/pets/decor · R19 末日遺跡城鎮 (multi-map `makeCamp`+`makeInterior`) · R20 ruin-town 2.5D art/decor collision/4 in-run events/data-driven boss moves/final 6 heroes (→27) · R20.1 apocalyptic title cover + V-versioned patch-notes · **R21** all 27 heroes get exclusives + co-op `mvLift` sync fix + dash-hint fix; **R21.1** maintenance cleanup; **R21.2** `__DBG.coopBossSyncTest()` offline boss/event sync test; **R21.8** story intro = blocking onboarding + frontend smoke CI gate + mobile keyboard hint · **R22** content codex (圖鑑石碑 station · `META.codex` discovery tracking · recipe reveal) + recommended goals (`content/goals.js`) + achievements focus tab + level-up card ◆ known-evolution pill · **R23** accessibility (shake 0-100% slider via `shakeUserScale` · flash/particle-density/damage-number toggles · shape+animation telegraphs) + assist mode (`META.assist` enemy hp/dmg/speed 50-100%, locked at run start, off in co-op, assisted runs skip ALL leaderboard uploads but keep gold/achievements).

Cloud setup: `server/README.md`; Oracle deploy (single canonical guide): `docs/DEPLOY.md`; multiplayer design + status: `docs/MULTIPLAYER_PLAN.md`. **CI/CD:** every push to `main` auto-deploys to Oracle (`.github/workflows/deploy.yml`).

## Run / test
- **Serve:** `node tools/serve.mjs` → http://localhost:5173 (a no-cache static server — do NOT use `python -m http.server`; it serves STALE ES modules after edits and silently breaks testing). `.claude/launch.json` exposes it as preview server "game".
- **Self-test in the browser:** `window.__DBG` exposes `snap()`, `reg()` (registry counts), `startRun()`, `autoplay(secs)`, `gallery()`, `enemyIds()`, `scene()`, `meta()`, `music()`, **`nav(name)`** (jump straight to `title`/`hub`/`run` — throttled rAF makes input-driven scene swaps unreliable), **`pump(n,dt)`** (manually tick N sim frames + one render), and the offline co-op self-tests **`coopRoundTrip()`** (encode→decode→render path), **`coopSilenceTest()`** (disconnect/retire), **`coopBossSyncTest()`** (R20 boss moves + 4 event mobs reconstruct on the guest).
- **Headless screenshots:** the preview tab is background-throttled, so `preview_screenshot` stalls. `tools/serve.mjs` exposes a dev-only `POST /__shot` that writes a base64 canvas capture to `_shot.png`; capture via `fetch('/__shot',{method:'POST',body:canvas.toDataURL('image/png')})` then Read `_shot.png`.
- **IMPORTANT — headless preview throttles `requestAnimationFrame`**, so `__DBG.autoplay` barely ticks and screenshots can be stale. Instead drive the sim manually from `preview_eval`:
  ```js
  const s = __DBG.scene(); s.player.takeDamage = () => {};      // godmode for inspection
  for (let i = 0; i < 9000; i++) { if (s.choice) s.choice = null; s.update(1/60); }
  ```
  Then read `s.run` / `s.world`. Call `s.render()` **sparsely** — it's expensive; rendering every tick can exceed the eval timeout. To screenshot a panel, set the field (`s.showBuild=true`, `s.bigMap=true`, a hub `s.panel='sortie'`) then call `s.render()` once.
- `window.__GAME_ERROR__` + the `#fatal` overlay surface uncaught errors.
- **Headless sim-test traps:** ① the first-battle `hudTut` walkthrough re-pauses the run ~2s in on a fresh save (`_hudTutShown` is per-scene) — clear `s.hudTut=false` INSIDE the pump loop or set `META.tutorialHUDDone=true`; since R21.8 the chapter `story` intro ALSO freezes the world for its first ~6.5s (solo only; skip by nulling `s.story` in the loop); ② anti-AFK drain writes `hp` directly (bypasses a `takeDamage` godmode stub) — a stationary godmode player still dies in ~4 min, so refill `s.player.hp` each frame; ③ `setScene()` queues — after calling it from `preview_eval`, run `__DBG.pump(1)` before `__DBG.scene()` or you read the OLD scene; ④ also null `s.equipChoice`/`s.eventChoice`/`s.curseChoice`/`s.anvilChoice` in pump loops (any of them freezes `run.update`).
- After editing, **reload the page** before testing — `preview_eval` runs the in-memory module, not your new file.
- **Frontend smoke (CI gate):** `cd test && npm ci && npm run test:frontend` — Playwright headless Chromium over `tools/serve.mjs` (needs :5173 free); asserts boot, `reg()` counts, nav/render, the story pause, the codex/goals layer, and the co-op self-tests (incl. codex pollution guard). Runs in CI (`frontend-test.yml`) and gates deploy alongside server tests. **When a content round changes registry counts, update `REG_BASELINE` in `test/frontend-smoke.mjs` in the same PR.** Test deps live only in `test/` — the game runtime stays zero-dependency.
- **Server tests** (`server/` has `type:module`): `node server/test/smoke.mjs` (103) + `node server/test/social.smoke.mjs` (65). `node --check` works in `server/` but NOT on frontend ESM (see Gotchas).

## Architecture
```
index.html                  Canvas + loader + global error trap
src/
  main.js                   boot: wire engine/content/scenes; 120Hz fixed-step loop; __DBG self-tests
  engine/                   game-agnostic
    loop.js (fixed-step)  math.js  input.js (keys+mouse+wheel, rebindable)
    renderer.js (camera / world<->screen / UI / shake)
    palette.js  sprites.js (Painter API + sprite registry)  particles.js  audio.js
  art/                      procedural sprites (defineSprite/defineAnim)
    core.js icons.js content_icons.js hub.js biomes.js biome_decor.js weapons.js heroes.js reaper.js lobby.js
    title_scene.js          R20.1 apocalyptic title cover
    town_floor.js town_church.js town_guildforge.js town_decor.js town_personal.js town_npcs_a/b.js town_outdoor.js town_pets_decor.js
    town_ruin_tiles/walls/facades/facades2/decor/interior/stations/stations2.js   ← R19/R20 ruin-town art
    gen/                    ← workflow-generated art (auto-loaded, fault-isolated)
  game/
    state.js                save/META, run lifecycle, base stats (makeBaseStats); cloud-sync seam
    scene.js  scenes/       title.js / hub.js + hub/*.js / run.js + run/*.js (hub & run = assembler + domain mixins) / coop.js (+ refs.js decoupler)
    world.js                tilemap collision, entities, combat, hazards, loot; makeCamp (ruin-town exterior) + makeInterior (interiors)
    player.js  enemy.js  projectile.js  pickup.js  hud.js  progression.js  maps.js  floor.js  toasts.js
    balance.js  status.js  cheats.js
    ui/                     settings.js (in-game settings/keybinds overlay) · gold.js (goldLabel pixel-coin renderer)
    net/                    coophost.js · coopbridge.js · protocol.js (co-op netcode)
    content/
      registry.js           ← THE integration seam (Enemies/Items/Equipment/Abilities/Talents/Facilities/Weapons/Characters)
      enemies/abilities/items/equipment/talents/facilities/weapons/characters/achievements/quests.js
      bonds·forge·guild·exclusives·events·npcs·skinshop·bank·town_gates·hidden(_rewards).js   ← town / meta systems
      curses·daily·boss_moves·event_mobs·heroes_r20·weapons_r20·patchnotes·pets·room_decor·lore.js   ← R18–R20 systems
      biome_tags·enemies_biome·bosses_biome·status_tags.js   ← biome affinity / final-boss / status data
      gen/                  ← workflow-generated content (auto-loaded)
  net/                      ← cloud client: api.js (fetch+JWT, offline-first) · ui.js (account bar + login/leaderboard/admin overlays) · rt.js (realtime WS) · social.js (friends/lobby)
tools/  serve.mjs  integrate.mjs  (_wf_*/_*.mjs are gitignored scratch)
server/                     ← cloud backend (Node+Fastify+Postgres+ws): src/server.js (buildApp factory + self-boot) · db.js · realtime.js · social.js · wsgw.js · test/
assets/music/               ← the 12 recorded soundtrack MP3s (state-mapped in engine/audio.js)
```

## Balance config (src/game/balance.js)
ALL difficulty / economy / pacing / status / map magic numbers live in `BALANCE` (one object) — tune there, not in gameplay code. Also exports `weaponMaxLevel`/`isWeaponMaxed` (weapons cap at **level 7**; character level is uncapped). Status effects live in `src/game/status.js` (`applyStatus`/`tickStatus`), enemy→player status tags in `content/status_tags.js`, achievement-gated unlocks in `content/unlocks.js`, the hidden dev cheat in `src/game/cheats.js` (Konami code → in-run dev panel; `window.__CHEATS`).

## Core loop (run.js)
**File layout (R21.4):** `run.js` is a thin assembler — `export const runScene = Object.assign({}, …mixins)` over `src/game/scenes/run/*.js` (`shared.js` = file-scope helpers/constants; then `lifecycle`/`modes`/`events`/`combat`/`loop`/`coop`/`render_hud`/`shop_hidden`/`render`/`overlays` mixins). The scene is still ONE `this`-bound object (state lives on `this`); add new methods to the matching mixin, not back into run.js. The only duplicate key, `exit()`, lives in `lifecycle` (early) + `modes` (full) — `modes` wins via Object.assign order, as in the original literal.
A run = **one biome, 20 minutes** (`BALANCE.LEVEL_TIME`) at a chosen **difficulty** (`run.difficulty`/`diffMul`).
- **Threat** `this.threat` ramps 1→~13 over 20 min (`BALANCE.THREAT_PERIOD`) and drives enemy tier + scaling. Only **1-3 enemy types** active at once (`rotateTypes`, ranged biased down).
- **Mini-bosses:** a DISTINCT boss at 5/10/15 min (`miniBossTick`/`spawnMiniBoss`, `BALANCE.MINIBOSS_TIMES`), never the biome final boss, never repeated.
- **Hazards** = `world.hazards` (HAZ table, dmg ×`TRAP_DMG_MULT`). **Special events** (`eventsTick`/`updateEvents`/`drawEvents`): mushrooms / surround ring / Higgs bombard + the 4 R20 event mobs (see Key subsystems), weighted via `BALANCE.EVENT_WEIGHTS`.
- **Status effects:** slow/bleed/burn/poison (DoT) + stun/knockup (CC) on player & enemies via `status.js`.
- **Finale:** at 20:00 the biome's `FINAL_BOSS` spawns; killing it → `clearLevel` (unlock next biome + difficulty). 30s later (`REAPER_DELAY`) the killable **Reaper** descends; slay it or press E to leave. `finishRun` banks once. (無盡 mode instead stacks curses past 20:00.)
- **Caps (no infinite pile-up):** weapons 6 / passives 14 (`MAX_WEAPONS`/`MAX_PASSIVES` in progression.js); weapon level 7; enemy hp/dmg + hazard/event damage scaling are clamped.
- Attack **tempo** ramps slow→fast (`world.playerTempo`/`enemyTempo`). Screen shake is dampened (`setShakeScale`, full only <25% HP).
- **Items/equip:** ground items auto-use on pickup (no inventory); equipment drops open a paused choose-to-equip menu (`equipChoice`). Anvils open a paused 3-choice (`anvilChoice`).

## Conventions when extending
- **Always add a changelog** — EVERY update/PR must add or extend a `docs/changelog/ROUNDx.md` entry (newest round) describing what changed + how it was verified. Don't scatter changelog `.md` files in the repo root; don't ship a change without one.
- **Content is data-driven** — register via `registry.js`. Never hard-code content in gameplay code; read from the registries. New magic numbers go in `BALANCE`.
- **New enemy:** `defineAnim('<sprite>', w, h, frames, (p,f)=>{ ...; p.outline(P.ink); }, {anchor:[ax,ay],fps})` then `Enemies.register({ id, sprite, ai:'chase|flyer|shooter|charger|wander', hp, speed, damage, radius, xp, gold, attack?, boss?, ... })`. Bosses (`boss:true`) auto-run the multi-phase system in enemy.js; wire data-driven moves via the `WIRE` map in `content/boss_moves.js` (NOT on the def — keeps gen defs re-integration-safe).
- **New weapon (auto-fire):** `Weapons.register({ id, icon:'weapon_<id>', tier, weight, maxLevel, cooldown(l), fire(world,p,inst), update?, draw?, levelDesc?, evolveInto?, evolveReq? })`. Evolves at maxLevel when the player owns `evolveReq` passive.
- **New character-exclusive weapon:** add a weapon-slot `Equipment.register({...,slot:'weapon',exclusive:true})` (hand-written `content/exclusives.js` uses an `X({...})` helper with a flat signature-weapon stat block) and map the owner in `CHAR_EXCLUSIVE`. Damage is applied at runtime via `makeEquipWeaponDef` — do NOT pre-multiply.
- **Icons:** `defineIcon('weapon_<id>'|'item_<id>'|'equip_<id>'|'ability_<id>', bgHex, (p)=>{...})`. **Character sprite:** `defineAnim('char_<id>',16,18,4,(p,f)=>{ drawHeroBody(p,f,id,art); p.outline(P.ink); },...)`.
- **Pixel API (`Painter`):** `px/rect/hline/vline/line/circle/ellipse/ring/mirrorX/outline/shadeBottom/replace`, plus polish helpers `glow/gradV/gradH/sparkle/star4/rimLight/softShadow/dither/speckle/aura` (rim light + soft shadow run AFTER the body, BEFORE `outline()`); shared palette `P` (anime accents `neon/magenta/sakura/aurora/astral/holy/rim` + ecosystem families `leaf/sand/bog/ocean/sky`), `sym` icon primitives. Missing sprites render a magenta placeholder (no crash); use `iconOr(name, fallback)`.
- **Stats** live in `makeBaseStats()` (state.js). Only write stat fields that exist there, or you'll get `NaN`.
- Equipment slots: `armor`/`trinket` apply stat mods; `weapon`-slot equips become a real **signature auto-weapon** via `makeEquipWeaponDef` (equipItem). Weapon-slot equips are sold in the in-run shop, not dropped.

## Workflow content pipeline
Content was mass-produced by multi-agent Workflows (generate → adversarial review). A workflow returns `{result:{packs:[{key,code,ids,spriteNames,ok}]}}`; normalize to `{result:packs}` then run **`node tools/integrate.mjs <file.json>`** — it decodes HTML entities, prepends the import header, writes `src/art/gen/*` + `src/game/content/gen/*`, and rebuilds the fault-isolated dynamic-import indexes by scanning the dirs. Re-run to integrate new output. **Gen files are overwritten on re-integration** (see Gotchas).

## Key subsystems
Topic-organized current architecture (per-round history is in `docs/changelog/`).

- **Hub / ruin town** (`world.js makeCamp`/`makeInterior`, `scenes/hub.js`): **File layout (R21.5):** like run.js, `hub.js` is a thin assembler — `Object.assign({}, …mixins)` over `scenes/hub/*.js` (`shared.js` helpers/constants; then `lifecycle`/`menus`/`panels`/`upgrades`/`render`/`render_smith`/`render_wardrobe`/`render_personal`); one `this`-bound object, add methods to the matching mixin. A multi-map 末日遺跡城鎮 — an irregular apocalyptic **exterior** (~72×54, ruin tilesets, width-3 streets, `triggers[]` walk-in door tiles, decor `solid:1|2` **player-only** collision folded into `world.block`/`solidTile`) + **6 building interiors** (`makeInterior(id)`, odd widths so `cx` is the true centre column). `hub.js` `this.area`/`this.maps`/`loadArea()` swap exterior↔interior via **step-on glow-circle doors** (E-near fallback). BOTH return the same `rooms{}` contract + **9 room ids** (church/guild/blacksmith/clothing/achievements/personal + plaza/garden/market). **Run biomes opt OUT** of the ruin tileset fields (`wallFace/wallCap/wallBands/voidTile` left unset) → they render byte-identically. Hub panels: `sortie` / `talents`(教堂) / `smith`(forge+facilities) / `guild`(quests+ranks) / `wardrobe` / `achievements` / `personal` / `codex`(R22 圖鑑石碑 plaza station). Every gold-spend routes through an `ask()` confirm modal; upgrade panels have a reset button that refunds spent gold.
- **Content codex & goals (R22)** (`content/codex.js` · `content/goals.js` · `hub/render_codex.js`): `META.codex={w,a,boss,rec}` object-maps record what the player has EXPERIENCED — hooks in `player.addWeapon`/`checkEvolve`/`fuseWeapons`, `abilities.applyAbility`, boss-death handlers, all guarded `!this.netInput` so a co-op host never records for guests (guest side records nothing — snapshot puppet). The 圖鑑石碑 plaza station opens a 5-tab panel (目標/武器/被動/Boss/配方); undiscovered = tinted silhouette (`drawSpriteUI` opt-in `tint`) + `unlockHintFor` hint; recipes stay `？？？` until first evolve/fusion (evolveReq + result never pre-revealed). `goalsFor(meta)` returns ≤3 progress-derived goals (guild claim > next biome/diff > near-done reward achievement > town gate). Level-up cards show a ◆ pill when the recipe is known; achievements panel defaults to a 聚焦 filter tab. When adding weapons with `evolveInto`, the codex recipe list picks them up automatically.
- **Town meta** (`content/{npcs,guild,forge,skinshop,bank,town_gates,pets,room_decor,hidden,hidden_rewards}.js`): ~10 NPCs (a "keeper" NPC sets `station:'<panelId>'` to route into a building panel); 12-rank guild (`GUILD_RANKS`; `bankRun` accrues xp, `claimGuildRank` grants one-time gold + unlock); out-of-run weapon **forge** (`meta.forge[id]={level≤5,effects≤3}`; `computeForgeMods` applied only WHILE that weapon fires, then restored; evolved/fused inherit the base instance's forge); **skin shop** (2 tabs, 30-min wall-clock reroll, hidden full-body skins via a `body()` override); **魂晶銀行** (borrow ≤ guild-rank limit, auto-repaid ×1.2 next run); **town feature gates** (forge/bank/talents/dojo gated by biome+guild progress; owned levels never stripped); **NPC 好感度** (daily +1, Lv1-5 rewards); local-only mini-pets; personal-room decor (gold sink); **hidden rooms** = discover-on-approach once-per-save rewards (`META.hidden.claimed`).
- **Heroes & art** (`art/heroes.js` + `content/characters.js` + gen hero packs + `content/heroes_r20.js`): every hero has a UNIQUE body — 18 archetypes in `heroes.js` via `registerHeroBody` (`HERO_MAP` maps core + g_* + h2_*); `h3_*` register their own in `gen_heroes3.js`; `h4_*` are inline in `heroes_r20.js`. `drawHeroBody(p,f,id,art)` falls back to `drawHunter`. Skins recolour the body + add an accessory (hidden skins fully override `body()`). **Load order matters** (see Gotchas).
- **Combat systems**: **bonds** (`content/bonds.js`, `checkBonds(run,player)` throttled in `run.update`); **character-exclusive weapons** (`content/exclusives.js` — `CHAR_EXCLUSIVE` map → injected into the OWNER's anvil only, ~32% via `rollGearChoice`; all 27 heroes have one as of R21); **status effects** (`status.js`); **damage attribution** (`world._curSrc` set around weapon/ability/DoT → `world.attributeDamage` → `run.dmgBySource` results ranking); **patron events** (`content/events.js`, mini-boss 3-choice named patrons with persistent hook effects); **4-tier rarity** (`RARITY`/`rarityOf`, swept through choice/anvil/shop/equip/tooltip/ground rings).
- **Boss moves & in-run events** (`content/boss_moves.js`, `content/event_mobs.js`): data-driven `BOSS_MOVES` (`leap_slam`/`wall_cage`/`charge_combo`/`shock_lines`) + `bossMoveTick()` hook in `enemy.js` (an active move owns the body for the frame). The central `WIRE` map keys moves by boss id so **gen boss defs stay untouched**. All telegraphs go through `world.addBeam` (the `bm` snapshot channel); `wall_cage` spawns destructible `boss_pillar` **ENTITIES** (no tile mutation → co-op-correct); `leap_slam` lift is `e.mvLift` (synced as enemy-tuple `t[8]` since R21). **Event mobs** are tier-9/weight-0 enemies (never in normal pools), gated by `BALANCE.EVENT_WEIGHTS`+threat: 自爆狂徒 / 魂晶詭雷 / 滾岩魔 / 寶藏哥布林. `__DBG.coopBossSyncTest()` self-tests the whole host→guest path offline.
- **Modes** (`content/{curses,daily}.js`): **無盡** stacks 12 curses past 20:00 (`bestEndlessTime`); **每日挑戰** = deterministic biome+hero+3 mutators from the date key (progress-isolated); **週常懸賞** = 9-of-3 ISO-week bounties. Server has mode-aware boards (`runs.mode`/`challenge_key`) + per-mode plausibility. Curses/mutators are **host-side scalars** → co-op protocol byte-unchanged.
- **Audio** (`engine/audio.js`): a streaming-`HTMLAudio` soundtrack layer (`TRACK_FILES` maps state→`assets/music/*.mp3`, `encodeURI` for CJK names, crossfade, per-track failure Set → synth fallback). `Music.setMode('title'|'hub'|'<biome>'|'miniboss'|'boss'|'reaper'|'victory'|'death')`. (Gotcha: `export const Audio` shadows the global `Audio` — use `document.createElement('audio')`.)
- **Cloud + co-op** — **host-authoritative relay**: one player's browser runs the unmodified `run.js` authoritative sim and broadcasts world snapshots (~18Hz); the Node server only relays. Frontend `src/net/` (`api.js`/`rt.js`/`ui.js`/`social.js`, offline-first — no-ops without a live JWT). Backend `server/` (Fastify+Postgres+`ws`; `buildApp(pool)` factory; score recomputed server-side in `/api/runs`; admin/moderation suite; `realtime.js` rooms/presence/relay with its OWN WS rate-limiter; `wsgw.js` binds `/rt`, JWT in `?token=`). Co-op netcode `src/game/net/` (`protocol.js` quantised tuples both ways — player `[x,y,faceX,hp,maxHp,flags,speed]`, enemy `[nid,defIdx,x,y,face,hpFrac,flags,scale,mvLift]`; `coophost.js` `CoopHost`; `coopbridge.js`). Guest scene `scenes/coop.js` = a puppet world driven by interpolated snapshots, with local prediction + reconciliation for the OWN avatar; it does NOT simulate. Multi-player is additive (`world.players[]` falls back to `[world.player]`; the host auto-resolves all menus since the shared world can't pause). Resilience: host migration, in-run reconnect (20s grace per cid), spectate, server score plausibility (`runPlausibility` 422-gate).

## Gotchas
- `KeyM` = **minimap**, `KeyB` = **shop** (not mute — mute lives in the settings menu). `Tab` = build page. `Shift` = dash (rebindable). Mouse `wheel` is wired (`mouse.wheel`); hub long panels have a draggable scrollbar.
- Save: key `soulshard.save.v1`, `SAVE_VERSION` + migration in `loadMeta`; reset backs up to `…v1.bak`.
- `node --check <file>` only checks syntax (won't resolve imports) — and treats the no-`package.json` frontend `.js` as CJS, so it **errors on `import`/`export`**; for frontend ESM, validate by reloading + the manual-pump test. (`server/` has `type:module`, so `node --check` works there.)
- **Gen-file edits get reverted by re-integration.** `src/game/content/gen/*` + `src/art/gen/*` are workflow-generated; several have been hand-edited (gen_weapons_a/b/c `BALANCE`/`PLAYER_DAMAGE_MULT`/crit/forge-swift fixes; `gen/talents.js`+`gen/facilities.js` cost MULs + value trims; `gen_items_anvils.js` diminishing returns; halved gold sources in a few gen ability/gear files). Re-running `integrate.mjs` overwrites them — re-apply, or patch the workflow source. Hand-written content files (exclusives, heroes_r20, weapons_r20, boss_moves, event_mobs, bonds, the co-op netcode, …) are NOT touched by re-integration.
- **Sprite bake is EAGER** (`defineAnim` runs the draw fn + caches frames at definition time). Any art a sprite depends on (e.g. `HERO_ART` bodies) must be registered BEFORE that sprite is defined. `characters.js` self-imports `art/heroes.js` so `HERO_ART` is populated before its eager bake loop; `state.js` transitively imports `content/characters.js` very early in boot, so character sprites bake before main.js's later art imports unless the dep is made explicit.
- Forge/grid rows that stash a non-rect payload in `r.w` must hit-test with a separate width field (e.g. `r.wd`) — `inside(mx,my,r)` reads `r.w` AS the width.
- **Forge 疾速/swift covers update-driven weapons:** `player.updateWeapons` stashes the effective haste on `inst.fmHaste` before calling `update()`; each update-driven weapon (beams/auras/turrets) divides its self-gating re-arm timers and per-enemy hit cooldowns by `(inst.fmHaste || 1)`.
- **Enemy proximity queries use a uniform spatial grid:** `world.rebuildGrid()` runs once per update; `world.forEachNear(x,y,r,cb)` replaces O(n²) scans in enemy separation, `nearestEnemy`, `dealAreaDamage`, and projectile→enemy combat. Grid cell = `TS*4`; clean at the 260-enemy cap.
- **Co-op:** `Player.maxHp` is a **getter** (`return this.stats.maxHp`) — never assign it; the guest stores the networked max as `pl.nmax`. `world.player` stays the LOCAL avatar; `world.players` is the full set. The WS path has its OWN rate limiter (`realtime._allow`) — `@fastify/rate-limit` only guards HTTP. The enemy tuple's last field is `mvLift` (t[8]); guest `makeEnemy` inits it to 0 and `applySnapshot` resets it each frame so the leap_slam lift releases cleanly.
- **Co-op testing:** a backgrounded/headless tab queues WebSocket `onmessage` until JS next runs — so `RT.room` lags between `preview_eval` calls; poll `RT.room.members.length` inside an active eval before asserting. Run only ONE dev server on :8787 (kill zombies via `taskkill //F //PID <netstat-pid>`). Prefer the offline self-tests `__DBG.coopRoundTrip()` / `coopSilenceTest()` / `coopBossSyncTest()` — they exercise the encode→decode→render path without two real tabs or a relay server.
- Git: commit messages via `git commit -F <file>` (PowerShell here-strings mangle `->`/parens). End commits with the Co-Authored-By trailer.
