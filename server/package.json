{
  "name": "soulshard-server",
  "version": "1.0.0",
  "description": "Soulshard Hunter — cloud backend (accounts + cloud save + shared leaderboard). Phase 1 of docs/MULTIPLAYER_PLAN.md.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node test/smoke.mjs && node test/social.smoke.mjs",
    "dev:fakedb": "node test/dev-fakedb.mjs",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/social.js && node --check src/realtime.js && node --check src/wsgw.js"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/rate-limit": "^10.2.2",
    "bcryptjs": "^2.4.3",
    "fastify": "^5.9.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "ws": "^8.21.0",
    "zod": "^3.23.8"
  }
}
